#include<iostream>
using namespace std;
int main(){
    int x=5;
    int y=7;
    int z;
    int w;
    w =min(x,y);
    z=max(x,y);
    cout<<w<<"\n";
    cout<<z<<"\n";
    //using the math header floor()
    //abs()
    //ceil()        means increas to upper but round to the upper
    //round()   just round
    //pow()
    //sqrt()
    // in hypotnous formula to find the hypotunos sqrt(pow(a,2)+pow(b,2))
}
