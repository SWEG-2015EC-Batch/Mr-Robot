//for loop can be used to cycle through the array for interactive data input
#include<iostream>
using namespace std;
int main(){
    double nums [][3]={{1,2,3},{4.5,6,7},{4,5,6}};
    for(int i=0;i<size(nums);i++){
        string numi;
        cout<<"enter raws number:";
        cin>>numi[i];
    }
    for(int i=0;i<size(nums);i++) {
        string numj;
        cout << "enter columns number:";
        cin >> numj[i];
    }
    return 0;
}
